import { QueryHighlight } from './query-highlight';

describe('QueryHighlight', () => {
  it('should create an instance', () => {
    const directive = new QueryHighlight();
    expect(directive).toBeTruthy();
  });
});
